<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.consumeradda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ChatDetailActivity"></activity>
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.CaseDetails" />
        <activity android:name=".activities.CasesList" />
        <activity android:name=".activities.ComplaintForm" />
        <activity android:name=".activities.Dashboard" />
        <activity android:name=".activities.support.ContactUs" />
        <activity android:name=".activities.support.Services" />
        <activity android:name=".activities.support.About" />
        <activity android:name=".activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.LoginActivity" />
    </application>

</manifest>